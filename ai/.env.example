# Copy to .env  (never commit .env itself)

# Optional: add a real Gemini API key to use Google's model for parsing.
# Without it, the built-in rule-based NLP parser is used — the demo works either way.
# GEMINI_API_KEY=

PORT=8000
