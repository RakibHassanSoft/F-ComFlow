# Copy to .env.local  (Next.js reads it automatically)
# Where the browser can reach the F-ComFlow API:
NEXT_PUBLIC_API_URL=http://localhost:4000

# One-click social connect (optional; manual connect works without these)
NEXT_PUBLIC_META_APP_ID=
NEXT_PUBLIC_META_WA_CONFIG_ID=
